# Expensive wildcard query

Leading-wildcard predicates commonly force sequential scans. Capture the exact query and plan;
consider a trigram index or query redesign only after checking existing indexes.

