# Upstream dependency outage

Confirm upstream timeouts across callers and check whether the dependency itself is unhealthy.
Use circuit breakers and failover when available. Escalate when ownership or blast radius is unclear.

