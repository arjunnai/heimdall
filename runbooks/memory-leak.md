# Service memory leak

Look for monotonic resident-memory growth, OOM warnings, and a deployment correlation. Roll back
the suspect version when supported by evidence. A restart is temporary mitigation, not root cause.

